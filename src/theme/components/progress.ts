import { StyleConfig } from '@chakra-ui/react'

export const Progress: StyleConfig = {
  baseStyle: {
    textStyle: 'body-75',
    bg: 'shading.100',
    filledTrack: {
      bg: 'primary',
    },
  },
}
